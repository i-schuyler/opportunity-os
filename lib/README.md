# lib/README.md

This directory is reserved for shared business logic, integrations, and utility helpers.

Current scaffolding:
- `auth-scaffold.js`: mock/dev-safe browser session helpers for sign-in shell
- `opportunity-model.js`: minimal opportunity record shape factory for v0.1
